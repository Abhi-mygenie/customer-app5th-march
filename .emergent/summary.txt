<analysis>**original_problem_statement**: 
The user initially requested bug fixes related to order flow, UI calculations, and editing active orders, leading to a structural refactoring to introduce TypeScript and a strict API Transformation layer. 
PRODUCT REQUIREMENTS: Transform raw external API data into strict internal models at the network layer to prevent frontend variable mapping bugs. Prevent user interaction with stale orders. 
*Recent Additions:* Ensure extensive codebase bloat is cleaned up safely, handle unique business logic for specific multi-menu restaurants (Restaurant 716), and fix POS token expiry issues causing the Admin QR Code generation to fail by implementing a more secure, session-based dual-token authentication flow.

**User's preferred language**: English
*(INSTRUCTION: Next agent must respond in English only)*

**what currently exists?** 
The app is stable on a mixed JS/TS architecture using a strict API Transformer pattern. In the latest session, significant code bloat was removed (~2,800 lines of unused Shadcn components). A critical hardcoded bypass was added for Restaurant 716 to allow multiple orders per table. A comprehensive  was authored. The Admin authentication flow was entirely re-architected: instead of relying on a permanently stored (and expiring) POS token in the database, the backend now concurrently authenticates the admin with the external POS API during login, returning a fresh  to the frontend. The frontend stores this locally and passes it via headers () to backend proxy routes like . Thorough markdown documentation is accessible via  endpoints.

**Last working item**:
    - Last item agent was working: Implementing and verifying the dual-token architecture for Admin login to fix QR code generation POS token expiry (BUG-031 / BUG-033).
    - Status: USER VERIFICATION PENDING (Agent verified logic, but user needs to test end-to-end with credentials).
    - Agent Testing Done: Y (Verified backend endpoints and frontend compilation).
    - Which testing method agent to use? Both (Frontend testing for login local storage, backend testing for  headers).
    - User Testing Done: N

**All Pending/In progress Issue list**:
Issue 1: QR Code URL empty when subdomain not set (BUG-029)
- Priority: P1
- Attempted fixes: Investigated. Found  is empty if POS API doesn't return  for tables. 
- Next debug checklist: Implement a warning message on the UI or add a manual subdomain override field in Admin Settings.
- Why fix this issue and what will be achieved with the fix? Ensures QR codes downloaded by admins have valid URLs pointing to the menu.
- Status: BLOCKED (Explicitly parked by user for a future session).
- Is recurring issue? N
- Should Test frontend/backend/both after fix? Frontend

**In progress Task List**: 
None.

**Upcoming and Future Tasks**:
*Upcoming Tasks:*
- P2-1: Fix Inclusive Tax Logic (Respect  from API so the app doesn't add tax on top of prices that already include it).
- P2-2: Restaurant-level Tax Settings (Support  and  payload fields).

*Future Tasks:*
- P3-1: Test Automation Implementation (Translate manual scenarios from  into Playwright E2E and Pytest API tests).
- P3-2: Remove  (A 1,300+ line file that is likely dead code superseded by the  tabs. Needs verification).
- P3-3: Extract Custom Hooks (, etc.) and Decompose . (Waiting for system stability).

**Completed work in this session**:
- Code Bloat Cleanup: Deleted 46 completely unused Shadcn UI components, saving ~2,862 lines of code. Updated .
-  updated with exact boolean usage tables for Variations and Add-ons.
- Fixed BUG-030 (Restaurant 716): Added critical hardcoding in  to skip table occupancy checks specifically for , allowing multiple active orders on one table.
- Created  with 87 detailed manual test cases spanning critical flows, UI/UX, and regressions. Added backend route to view it.
- Fixed BUG-031/033 (POS Token Expiry on QR Page): 
  - Modified   to call the POS API () and retrieve a fresh POS token.
  - Modified frontend  and  to store both  (local JWT) and  in .
  - Modified  to pass  via the  header.
  - Modified  to consume the  header instead of hitting MongoDB for a stale token.
- Fixed BUG-032: Addressed a TypeScript error in  by adding an  type assertion for .

**Earlier issues found/mentioned but not fixed**:
- QR Code URL Issue (Parked by user, documented as Issue 1 above).

**Known issue recurrence from previous fork**:
- Issue recurrence in previous fork: None
- Recurrence count: 0
- Status: NA

**Code Architecture**:


**Key Technical Concepts**:
- **Transformer Pattern**: External API data is normalized into strict internal models via  before hitting React.
- **Dual-Token Auth Flow (Admin)**: The app uses  (JWT) for internal backend routes, and  (fetched on login, stored in localStorage) for POS-proxied backend routes (like QR code generation).
- **Multi-Menu & Hardcodings**:  changes payload shapes. Restaurant 716 has hardcoded behavior to skip table occupation checks.

**key DB schema**:
- : Contains restaurant admin details. **CRITICAL CHANGE:** We no longer rely on  stored in this collection for active POS API calls. It was causing token expiry bugs. 

**changes in tech stack**:
- Architectural shift regarding authentication: POS API tokens for admin actions are now treated as transient session tokens (stored in localStorage) rather than persistent DB fields.

**All files of reference**:
1.  (Auth flow,  modified)
2.  (Token storage)
3.  (Logout token clearing)
4.  (Token headers, Token expiry UI states)
5.  (Restaurant 716 hardcoding)
6.  (Newly created test repository)
7.  (Updated with Critical Hardcodings section)

**Areas that need refactoring**:
-  (1600+ lines).
-  (1300+ lines, suspected dead code).
-  needs conversion to TypeScript (currently causes minor TS assertions in ).

**key api endpoints**:
-  (Local backend login - now returns  alongside )
-  (Backend proxy - now requires  header)
-  (External POS Admin login endpoint used by our backend).

**Critical Info for New Agent**:
- **Dual-Token Architecture:** If an admin-facing backend route needs to communicate with the external POS API, it MUST extract the token from the request headers ( sent by the frontend), *not* from the  collection.
- **Restaurant 716 Bypass:** If investigating table status bugs, be aware that  explicitly bypasses occupied table checks for .
- **TypeScript Webpack Quirk:** When moving  files to , ensure you don't leave duplicate  files around, as Webpack will silently favor the older  files.

**documents and test reports created in this job**:
-  (New)
- Updates to , , , , 

**Last 10 User Messages and any pending HUMAN messages**:
1. User asked where  data is used. (Agent mapped out the endpoints).
2. User verified that fixing the Admin POS token won't break the Customer silent auth. (Agent confirmed they are parallel, separate flows).
3. User clarified the exact desired architecture: Admin login fetches token from POS -> stores in local storage -> uses local storage token for QR page. No DB storage.
4. User confirmed understanding of the flow where our backend handles the two API calls simultaneously during login.
5. User asked if  (JWT) was already present and used. (Agent confirmed it was already there and used for internal backend routes).
6. User asked for specific endpoints where  is used. (Agent listed them out).
7. User confirmed understanding of the dual token (Internal JWT vs POS Token).
8. User asked what specific code changes are needed. (Agent outlined the 7 changes across 4 files).
9. User approved the changes and requested doc updates. (Agent executed the code changes).
10. *Pending*: User needs to test the newly implemented Admin login and QR page flow using the provided credentials.

**Project Health Check**:
- Broken: None currently blocking. (QR URL subdomains are conditionally empty but parked).
- Mocked: Customer POS auth still uses a hardcoded phone/password in  (working as intended by previous design).

**3rd Party Integrations**:
- MyGenie POS API (Requires User credentials, dynamically handled). No Emergent LLM Keys used.

**Testing status**:
- Testing agent used after significant changes: NO (User prefers manual verification for this specific architectural auth change).
- Troubleshoot agent used after agent stuck in loop: NO
- Test files created:  (Manual test definitions).
- Known regressions: None identified.

**Credentials to test flow**:
- Admin UI testing:  /  (Required to verify the POS token refresh and QR code generation).

**What agent forgot to execute**:
All requested items were analyzed, debated, and executed precisely according to the user's architectural instructions.</analysis>
